import React, { useState, useEffect } from 'react';

function ReservationManagement(props) {
  const {
    user,
  } = props;

  return (
    <div className='ReservationManagement'>
      <h1>Reservation Management</h1>
    </div>
  );
}

export default ReservationManagement;
